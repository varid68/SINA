import React from 'react';
import { View, Text } from 'react-native';

const StatusBarComp = () => (
  <View style={{ backgroundColor: '#4caf50', height: 25 }}>
    <Text />
  </View>
);

export default StatusBarComp;
